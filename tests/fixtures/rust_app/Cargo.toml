[package]
name = "sensor-gateway"
version = "0.2.0"
edition = "2021"
rust-version = "1.75"

[dependencies]
tokio = { version = "1.38", features = ["full"] }
serde = { version = "1", features = ["derive"] }
anyhow = "1.0"

[dev-dependencies]
proptest = "1.4"
